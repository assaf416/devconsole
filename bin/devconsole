#!/usr/bin/env ruby
# frozen_string_literal: true
# encoding: utf-8

# Locate the gem lib directory regardless of install location
$LOAD_PATH.unshift(File.expand_path('../lib', __dir__))

require 'devconsole/cli'

DevConsole::CLI.start(ARGV)
